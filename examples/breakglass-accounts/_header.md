# Custom domains example

This example creates two emergency access accounts (break-glass accounts) and a security group containing both. The security group can be used to exclude those accounts from Conditional Access:
