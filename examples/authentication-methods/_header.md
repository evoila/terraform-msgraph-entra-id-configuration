# Authentication methods example

This example configures the tenant's authentication methods policy: enabling FIDO2 (passkeys) with
two distinct passkey profiles, and explicitly disabling SMS as an authentication method:
